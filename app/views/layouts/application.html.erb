<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %> | <%= t("static_pages.application.title").html_safe %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
